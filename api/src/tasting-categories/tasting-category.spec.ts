import { TastingCategory } from '../tastings/tasting-category.entity';

describe('TastingCategory', () => {
  it('should be defined', () => {
    expect(new TastingCategory()).toBeDefined();
  });
});
