import { PicturesCategory } from '../pictures-category/pictures-category';

describe('PicturesCategory', () => {
  it('should be defined', () => {
    expect(new PicturesCategory()).toBeDefined();
  });
});
