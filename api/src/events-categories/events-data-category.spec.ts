import { EventsDataCategory } from './events-data-category.entity';

describe('EventsDataCategory', () => {
  it('should be defined', () => {
    expect(new EventsDataCategory({})).toBeDefined();
  });
});
